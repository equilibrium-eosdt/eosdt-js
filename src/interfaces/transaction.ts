export interface ITrxParamsArgument {
    permission?: string
    blocksBehind?: number
    expireSeconds?: number
}

export interface ITrxParams {
    permission: string
    blocksBehind: number
    expireSeconds: number
}
